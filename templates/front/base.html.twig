<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="{{asset('css/style.css')}}">
    <title>Document</title>
</head>
<body>
<header>
<nav>
				<a href="{{ path('comic_list' )}}">comics</a>
                <a href="{{ path('designer_list' )}}">designers</a>
				<a href="{{ path('editor_list' )}}">editors</a>
				<a href="{{ path('licence_list' )}}">licences</a>
				<a href="{{ path('writer_list' )}}">writer</a>
				{# <a href="{{ path('admin_writer_list' )}}">user</a> #}
    
			</nav>
		</header>

<div class="glob-const">
		<h1>
			{{ front }}
		</h1>
	</div>

{% for grlicence in glicences.getAll %}
			<h4>{{ grlicence.name }}</h4>
		{% endfor %}
  
  {% for greditor in geditors.getAll %}
			<h4>{{ greditor.name }}</h4>
		{% endfor %}

{% block main %}

{% endblock %}
    
</body>
</html>