{% extends "/admin/base.html.twig" %}



{% block main %}

{# {{form(designerForm)}} #}

	{{form_start(designerForm,  {'attr' : {'class' : 'form'}})}}

	{# création d'un label #}
	{{ form_label(designerForm.name, 'Name du designer', {'label_attr' : {'class' : 'label'} } ) }}

	{#création des champs avec form_widget#}
	{{ form_widget(designerForm.name, { 'attr' : {'placeholder' : 'Name du designer', 'class' : 'title' } } )}}


	{{ form_label(designerForm.country, 'Country du designer', {'label_attr' : {'class' : 'label'} } ) }}

	{{ form_widget(designerForm.country, {'attr' : {'placeholder' : 'Country du designer', 'class' : 'content' } } ) }}

	

	{{form_widget(designerForm.enregistrer, {'attr' : {'class' : 'btn'}})}}

	{{form_end(designerForm)}}

{% endblock %}