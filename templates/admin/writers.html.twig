{% extends "/admin/base.html.twig" %}


{% block main %}

{% for writer in writers %}

<div class="card">

<a href="{{ path('admin_writer_show', {'id' : writer.id}) }}">{{ writer.name }}</a>
    
 </div>
    
{% endfor %}
{% endblock %}

